import "./WorkModal.css"

const CreateWorkModal = ({ setOpenModal, modalBackground }) => {

    const closeCreateTeamModal = (e) => {
        setOpenModal(false);
    }

    return (

        <>
        </>
    )
}

export default CreateWorkModal;